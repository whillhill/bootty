# bootty (npm main package)

This package only provides the `bootty` command entrypoint. It loads a prebuilt, platform-specific package for your OS/CPU.

If you need to debug installation issues, run:

```bash
bootty --help
```
